#lang sicp

; Доказательство зависимости радиуса интервала от аргументов суммы
; интервал1 = a1 b1
; интервал2 = a2 b2
; r1 = (b1 - a1) / 2
; r2 = (b2 - a2) / 2
; r1 + r2 = (b1 - a1) / 2 + (b2 - a2) / 2 = ((b1 - a1)  + (b2 - a2)) / 2 = ((b1 + b2)  - (a1 + a2)) / 2 
;
; sum = (a1 + a2) (b1 + b2)
; sum_r = ((b1 + b2) - (a1 + a2)) / 2
;
; => r1 + r2 = sum_r